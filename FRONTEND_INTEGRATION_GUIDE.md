# Frontend Integration Guide

The existing frontend uses:

```txt
env-config.js -> app-config.js -> api-client.js -> window.VSS_API.submitLead()
```

The endpoint is:

```txt
/api/submit
```

React/Vite-compatible code and examples are included in:

```txt
docs/PRODUCTION_FORM_SYSTEM.md
```
